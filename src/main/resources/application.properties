spring.application.name=correosUAP

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/correos_uap}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:123456789}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:soporte@uap.edu.bo}
spring.mail.password=${MAIL_PASSWORD:iqctdianczektlbz}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.mail.from=${MAIL_FROM:${MAIL_USERNAME:no-reply@uap.local}}
app.mail.logo-url=${MAIL_LOGO_URL:https://uap.edu.bo/wp-content/uploads/UAP-250px-1.png}
